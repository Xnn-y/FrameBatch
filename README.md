# FrameBatch

FrameBatch is a Windows desktop tool for batch video cover extraction and frame-removal workflows powered by FFmpeg.

Current status: phase 0 project skeleton.

## Development

```powershell
python -m pip install -e ".[dev]"
python -m pytest
python -m framebatch.main
```
